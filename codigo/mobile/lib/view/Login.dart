import 'package:flutter/material.dart';

// not implemented yet
class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ficha Médica Digital'),
      ),
    );
  }
}